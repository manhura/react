describe('First test', () => {
  it('1st test', () => {
    expect(1 + 2).toEqual(3);
  })

  it('Test 2', () => {
    expect(0 + 4).toEqual(4);
  })

  it('Test 3', () => {
    expect(0 + 4).toEqual(4);
  })
});